# 广告过滤 App · 使用说明

这是一个**你自己拥有**的安卓广告过滤工具，源码完全在你手上，不含任何第三方 SDK、不联网上传任何数据。

---

## 一、它到底是什么

简单说：**它在你手机里开了一个"假的 DNS 服务器"，专骗广告请求。**

```
你打开运动世界
    ↓
App 想加载广告 → 去问 DNS："pangolin.snssdk.com 在哪？"
    ↓
我们的过滤器截住这个询问
    ↓
查黑名单 → 命中！
    ↓
回答："这个地址是 0.0.0.0"（一个永远连不上的地址）
    ↓
广告 SDK 连不上服务器 → 广告加载失败 → 你看到的是空白或直接跳过
```

而**正常的上网请求**（刷网页、看视频、地图导航）不命中黑名单，原样转发给真实 DNS，完全不受影响。

### 为什么这样设计

| 对比 | 改包去广告 | 这个 App |
|---|---|---|
| 需要破解加固 | 要（很难） | 不需要 |
| 需要 root | 有时要 | 不需要 |
| 需要装证书 | 有时要 | 不需要 |
| 会不会让 App 崩溃 | 容易崩 | 不会，广告只是加载不出来 |
| 法律风险 | 有 | 无（只管理你自己设备的网络） |
| 原理是否可写进简历 | 不能 | 能 |

**关键点**：它只接管 DNS 查询，**不接管你的上网流量**，所以不会拖慢网速，也不会看到你的任何数据内容。

---

## 二、安装

### 方法 A：用数据线（推荐）

1. 手机开启 **USB 调试**（设置 → 关于手机 → 连点"版本号"7 次打开开发者选项 → 返回 → 开发者选项 → USB 调试）
2. 数据线连电脑
3. 在本目录打开命令行，执行：

```
toolchain\platform-tools\adb.exe install -r app\output\AdFilter.apk
```

### 方法 B：不用数据线

1. 把 `app\output\AdFilter.apk` 通过微信/QQ/蓝牙发到手机
2. 手机上点开安装
3. 如果提示"未知来源"，按提示允许即可（这是正常的，因为不是从应用商店装的）

---

## 三、使用（三步）

### 第 1 步：打开 App

桌面会出现一个叫 **"广告过滤"** 的图标，点开。

界面上有：
- 一个大按钮（启动/停止）
- 状态与拦截计数
- 6 个规则勾选框：穿山甲、腾讯、快手、百度、华为荣耀、**公共广告规则集（18.4 万条）**，默认全开。
  勾选状态会自动记住，下次打开还是这样。
- **防误杀**区：白名单管理入口、拦截返回方式切换、开机自启开关
- 下方的拦截日志 / 域名排行

> 公共规则集十几万条，覆盖广但也可能"用力过猛"。发现哪个 App 用不了，
> 去"白名单管理"里点一下那个域名就恢复了 —— 见第五节之六。

### 第 2 步：点"启动过滤"

**第一次点会弹出系统对话框**，大意是"是否允许此应用建立 VPN 连接"。

> ⚠️ 这个弹窗看起来有点吓人，但必须点 **确定 / 允许**。
> 这不是真的 VPN，只是安卓系统要求：任何接管网络的 App 都必须走这个授权流程。
> 我们的 App **不会**把你的流量发到任何服务器 —— 代码里唯一的对外请求是把 DNS 查询转发给 `223.5.5.5`（阿里公共 DNS），你可以打开源码自己确认。

授权后，状态会变成 **"运行中"**，通知栏出现一个"广告过滤已开启"的常驻通知（防止被系统杀掉）。

### 第 3 步：去验证效果

打开运动世界，看开屏广告是否还出现。

回到过滤 App，你会看到：
- **已拦截 XX 次** 的数字在涨
- 日志里出现类似 `拦截 pangolin.snssdk.com` 的记录

如果数字在涨、广告没了 —— 成功。

---

## 四、重要：先关掉"私人 DNS"

安卓 9 以上有个**私人 DNS**功能，开启后系统会用加密 DNS，**绕过**我们的过滤。

**必须关掉**：

```
设置 → 网络和互联网（或"连接"）→ 私人 DNS → 选择"关闭"
```

不同手机位置略有差异，找不到就在设置里搜"私人 DNS"或"DNS"。

如果没关，你会发现拦截计数一直是 0 —— 这是最常见的问题。

---

## 五、常见问题

**Q：拦截计数一直是 0？**
先检查"私人 DNS"是否关闭（见上）。其次确认目标 App 确实在请求广告域名。

**Q：广告还是出现？**
可能原因：
1. 该广告走的是 IP 直连或 HTTPDNS（不经过域名解析），DNS 过滤拦不住 —— 这是本方案的固有局限
2. 广告域名不在黑名单里 —— 看日志里放行了哪些域名，把新的加进 `BlockList.java`
3. 广告是 App 本地内置的（图片写死在包里），不是从网络拉的 —— 这种任何网络方案都拦不住

**Q：会不会影响正常上网？**
不会。App 只处理 UDP 53 端口的 DNS 查询，其他流量根本不经过它。

**Q：耗电吗？**
几乎不耗。只处理 DNS 包，数据量极小。

**Q：不想用了怎么办？**
回到 App 点"停止过滤"，或直接卸载。不会有残留。

**Q：为什么叫 VPN 但又不加密？**
安卓系统只有 `VpnService` 这一个接口能让普通 App 接管网络。我们借用了这个通道，但只用它读 DNS 询问，不做任何代理或加密。

---

## 五点四、怎么判断一个域名是不是广告（重要）

看域名排行时会遇到大量不认识的域名。按下面四类分：

| 类别 | 特征关键词 | 拦掉的效果 |
|---|---|---|
| **广告请求** | `gdt` `snssdk` `pangle` `kwad` `mobads` `cpro` `adx` `oceanengine` | ✅ **广告真的会消失** |
| **素材 CDN** | `img` `cdn` `static` `p0.` `byteimg` `gtimg` `alicdn` | 可能是广告素材，也可能是正常图片，需确认 |
| **日志统计** | `log` `ulog` `xlog` `metrics` `stat` `track` `report` `upc` | ❌ **广告照常显示**，只是不上传行为数据 |
| **功能服务** | `api` `push` `login` `pay` `map` `amap` `passport` | ❌ 千万别拦，会让 App 功能异常 |

**最容易搞错的一点**：看到 `xlog` `ulog` `metrics` 以为是广告，其实那是日志上报。
拦掉它广告一个都不会少，只是 App 不再向厂商汇报你的行为。

### 批量判断工具

把域名存成 `domains.txt`（每行一个，也支持从 App 排行里直接复制的 `域名 x12 [放]` 格式），然后：

```
python tools\analyze_domains.py domains.txt
```

会自动分类并给出建议，结果也在 `_domain_report.txt`。

### 判断不准时怎么办

1. **搜域名**：把域名粘到搜索引擎，通常能在某 App 的《第三方信息共享清单》里找到归属
2. **看时机**：广告弹出那一刻突然出现的域名，基本就是广告
3. **试拦**：拦掉后打开 App 看功能是否正常，异常就取消

---

## 五点五、广告没拦干净怎么办（重要）

DNS 过滤只能拦住**写进黑名单的域名**。如果漏了，说明那个域名不在名单里。

### 用 App 自带的诊断找出漏网域名

界面上有个切换按钮，点一下会变成 **「当前：域名排行」**，显示所有被查询过的域名，按次数排序，每条标着拦或放：

```
pangolin.snssdk.com     x12   [拦]
xxxx.cdn.com            x8    [放]     ← 这类就是漏网的
```

**做法**：启动过滤 → 打开目标 App 多刷几个有广告的页面 → 切到域名排行 → 把标 `[放]` 的域名记下来，补进 `BlockList.java` 重新编译。

这是最准的办法，比任何通用规则集都贴合你手上的 App。

### 两种拦不住的情况

| 情况 | 表现 | 能不能解决 |
|---|---|---|
| 域名不在黑名单 | 排行里能看到，标 `[放]` | 能，加进黑名单即可 |
| SDK 用 HTTPDNS 或直连 IP | 排行里**根本看不到**广告域名，但广告照常出现 | 不能，DNS 方案原理上够不着，需升级到 VPN+SNI |

第二种情况意味着广告 SDK 绕过了系统 DNS，改走自己的 HTTP 接口拿 IP。如果出现这种，就需要接管全流量、读 TLS 握手的 SNI 字段来拦 —— 那是另一个量级的工程。

---

## 五点六、防误杀（重要）

规则越多，误伤正常功能的概率越高。四条防线，从上到下优先级递减：

### ① 快捷开关：出问题一秒关闭

安装后，**下拉状态栏** → 点右上角铅笔/编辑 → 在"可用磁贴"里找到 **广告过滤** → 拖到常用区。

之后不用打开 App，**下拉点一下**就能开关过滤。

> 严格来说第三方 App 没法往系统「设置」里插自己的页面（那是系统签名才有的权限），
> 快捷设置磁贴是官方留给三方 App 的标准入口，体验上最接近"集成进系统"。

### ② 白名单：无条件放行

主界面 → **白名单管理**。白名单命中的域名，任何规则都拦不住。

界面里有两块：

- **自定义白名单**：手动添加，支持精确 / 后缀匹配（写 `example.com` 会连带放行其所有子域）
- **最近被拦的域名**：直接从运行记录里捞出来的，每条下面标着"是哪家规则拦的"，**点一下就进白名单，立刻生效**

典型排障路径：

```
某个 App 突然用不了
    → 打开白名单管理
    → 看"最近被拦的域名"里有没有它的域名
    → 点一下 → 恢复
```

> ⚠️ 白名单的后缀匹配是双刃剑：写 `qq.com` 会连带放行 `gdt.qq.com`（腾讯广告）。
> 所以内置条目都写得很精确，你自己加的时候也尽量写到二级或三级域名。

### ③ 拦截溯源：看清楚是谁拦的

日志和域名排行现在会带上**命中的具体规则**，格式是 `[拦←公共规则集: xxx]`。

有了它，误杀不再靠猜：一眼就能看出是"你自己写的规则"拦的（一般没问题），
还是"公共社区规则"拦的（嫌疑最大，加白名单）。

### ④ 拦截返回方式：NXDOMAIN / 0.0.0.0

主界面可以切换 App 怎么"骗"广告 SDK：

| 方式 | 做法 | 特点 |
|---|---|---|
| **域名不存在**（默认，推荐） | 回一个 NXDOMAIN | App 立刻失败处理，反应最快、不卡顿 |
| **解析到 0.0.0.0** | 回 A 记录 `0.0.0.0` | 部分 App 会一直重试到超时，表现为转圈 |

个别 App 在 NXDOMAIN 下状态不对时，切到 0.0.0.0 试试。

### ⑤ 开机自启（默认关）

主界面勾选 **开机自动启动过滤** 后，重启手机不用再手动开。

默认关闭是有意为之：Android 各版本对"开机后启动前台服务"的限制一直在收紧，
某些 ROM 会直接拦下来。开着如果没生效，手动点一下就行，不影响别的功能。

---

## 五点七、隐身与通知

### 后台干净

这个 App 不会出现在「最近任务」列表里 —— 划开后台看不到它。
过滤服务照常在跑，只是不占一格后台卡片。

### 常驻通知

启动过滤后，通知栏会出现一条：

```
广告拦截正在运行
已拦截 1234 次　放行 5678 次
                        [停止]
```

- 计数每 6 秒刷新一次，点标题回到 App 界面
- 右边有 **停止** 按钮，不用打开 App 就能关

> Android 13+ 第一次启动会弹通知权限申请，**必须允许**。
> 否则通知不显示，隐藏图标后你就找不到这个 App 了。

### 隐藏桌面图标（可选）

主界面 → 隐身 → 勾选 **隐藏桌面图标**，图标就从桌面消失了。

之后从这里进：

1. **下拉通知栏**，点那条「广告拦截正在运行」的通知
2. **系统设置 → 应用 → 广告过滤 → 打开**
3. 下拉快捷设置里的 **广告过滤** 磁贴（只能开关，不进界面）

> ⚠️ 界面里会检查你是否已给通知权限。**没给权限时这一项会红字警告**，
> 因为那种情况下隐藏图标 = App 弄丢。
>
> 万一真弄丢了，用数据线执行这条命令恢复：
> ```
> toolchain\platform-tools\adb.exe shell pm enable com.lab.adfilter/.LauncherAlias
> ```

---

## 五点八、耗电优化

第一版实测：一晚上 + 一上午掉了约 1150mAh，电量曲线肉眼可见地陡。原因已定位并修复。

### 为什么"只过滤 DNS"也会费电

关键认知：**耗电来自唤醒次数，不是流量大小。**

旧实现每来一个 DNS 查询就 `new DatagramSocket()`、发完立刻 `close()`。
一台手机一晚上几千次查询，就是几千次 socket 创建销毁 —— 每次都要
拉起内核网络栈、重走一遍路由选择，在 VPN 场景下还要再穿一遍 tun。
这点开销单次微不足道，累积起来就是主要耗电源。

### 都改了什么

| 项 | 改动 |
|---|---|
| **socket** | 常驻复用，不再每次新建；上游 DNS 地址也只解析一次并缓存 |
| **通知刷新** | 2.5 秒 → **6 秒**一次 |
| **界面轮询** | 主界面 700ms → 1 秒；白名单界面 1 秒 → 2 秒 |
| **日志** | 默认只记拦截/白名单放行，普通放行不记（界面有「全量日志」开关） |
| **域名统计** | 加 3000 条上限，防止长期运行内存无限膨胀 |

### 还可以再省一点

- **关掉用不到的规则分组**：主界面把「公共广告规则集（18.4 万条）」之外的
  用不上的分组取消勾选，匹配时逐条比对的范围小了，CPU 唤醒时间也短
- **不用的时候直接关**：下拉通知栏点「停止」，或点快捷磁贴

### 如果还是费电

更彻底的方案是**不在这台手机上跑任何 App**：

自建一个 DoT（DNS over TLS）服务器放到常开的机器上，然后把手机的
**设置 → 私人 DNS** 填成这个服务器的域名。全系统生效、零常驻进程、
零唤醒 —— 因为过滤逻辑跑在服务器上，手机只是发个普通的加密 DNS 查询。

代价是需要一台常开的设备（电脑/软路由/VPS）+ 一个公网域名 + 受信任的 TLS 证书。
需要的话可以继续帮你做这个方向。

---

## 六、改规则（进阶）

所有规则写在源码里，改完重新编译即可。

文件：`app\src\com\lab\adfilter\BlockList.java`

```java
public static final String[][] GROUP_DOMAINS = {
    // 穿山甲
    { "snssdk.com", "pstatp.com", "bdurl.net", "pglstatp-toutiao.com" },
    // 腾讯优量汇
    { "gdt.qq.com" },
    ...
};
```

匹配规则：写 `snssdk.com` 会拦住它**及其所有子域**（`i.snssdk.com`、`pangolin.snssdk.com` 等）。

想加新域名，往对应数组里加字符串就行。想加新分组，改 `GROUP_NAMES` 和 `GROUP_DOMAINS` 两个数组（长度保持一致）。

---

## 七、重新编译

改完源码后：

```
python app\build.py
```

编译脚本会依次执行 `aapt2 → javac → d8 → zipalign → 签名`，最终产物在：

```
app\output\AdFilter.apk
```

**不需要 Android Studio，也不需要 Gradle。** 只用到 Android SDK 的命令行工具，已经装在 `toolchain\android-sdk\` 下（约 120MB）。

编译时遇到问题可以看 `app\build\build_log.txt`。

---

## 八、源码结构

```
app/
├── AndroidManifest.xml                  权限与组件声明（含磁贴、开机广播）
├── res/values/strings.xml
├── assets/blocklist.txt                 18.4 万条公共规则（编译进 APK）
├── src/com/lab/adfilter/
│   ├── MainActivity.java                主界面：开关/规则/统计/日志/防误杀
│   ├── AdFilterVpnService.java          ★核心：VpnService + DNS 解析与拦截
│   ├── BlockList.java                   广告域名黑名单（含 matchRule 拦截溯源）
│   ├── AllowList.java                   白名单：内置保底 + 自定义，优先级最高
│   ├── AllowActivity.java               白名单管理界面 + 一键放行
│   ├── AppSettings.java                 设置持久化（开机自启、拦截方式、分组）
│   ├── AdFilterTileService.java         下拉快捷设置磁贴
│   └── BootReceiver.java                开机自启 + 升级后恢复
├── build.py                             编译脚本
└── output/AdFilter.apk                  成品
```

想搞懂原理，重点读 `AdFilterVpnService.java`，关键就四处：

1. **`establishVpn()`** —— 建立 VpnService，只声明虚拟 DNS（`10.0.0.1`），不劫持全部流量
2. **`handlePacket()`** —— 从 IP 包里剥出 UDP，剥出 DNS，取出域名，
   按「白名单 → 手工分组 → 公共规则集」的顺序判定
3. **`buildBlockResponse()`** —— 伪造 DNS 应答，NXDOMAIN 或 A=0.0.0.0
4. **`writeUdp()`** —— 手写 IP/UDP 校验和后写回 tun

---

## 九、下一步可以做什么

现在这个是 **DNS 层**过滤。想更进一步，可以做 **VPN + SNI** 过滤：

- 把 `addRoute("0.0.0.0", 0)` 打开，接管全部流量
- 解析 TCP 里的 TLS 握手包，读 **SNI 字段**（明文，不需要解密）
- 命中黑名单就断掉连接，否则做 NAT 转发

那是 AdGuard 高级版的核心机制，也是个能拿去面试讲的完整项目。

需要的话我可以继续帮你往下做。
